# Patch to add qa-specific tags to ALB Ingress
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend-ingress
  annotations:
    alb.ingress.kubernetes.io/tags: Environment=qa,Project=microservices,ManagedBy=kustomize
