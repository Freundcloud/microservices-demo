# Patch to add prod-specific tags to ALB Ingress
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend-ingress
  annotations:
    alb.ingress.kubernetes.io/tags: Environment=prod,Project=microservices,ManagedBy=kustomize
